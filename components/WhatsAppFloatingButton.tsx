"use client";

// The 31-32 WhatsApp community link is not available yet, so this floating
// button is hidden. Restore the anchor (and set the real invite URL) once it
// is provided.
export default function WhatsAppFloatingButton() {
  return null;
}
